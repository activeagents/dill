{
  "type": "function",
  "name": "extract_text",
  "description": "Extract text content from the current page or a specific element.",
  "parameters": {
    "type": "object",
    "properties": {
      "selector": {
        "type": "string",
        "description": "CSS selector of the element to extract text from. Defaults to 'body' for the entire page."
      }
    }
  }
}
