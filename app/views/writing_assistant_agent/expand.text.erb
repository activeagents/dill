Please expand the following<%= @has_selection ? " selected" : "" %> text with more detail and depth:

<% if @related_content.present? %>
## Related sections from this report (use for context and to maintain consistency):
<% @related_content.each do |related| %>
### <%= related[:title] %>
<%= related[:content] %>

<% end %>
<% end %>
<% if @reference_context.present? %>
## Reference sources to incorporate:
The following sources were found in the selected text. Use them to add accurate details and examples:

<%= @reference_context %>

<% end %>
<% if @has_selection %>
## Selected text to expand:
<%= @selection %>

<% if @full_content.present? && @full_content != @selection %>
## Full document context (for reference - helps maintain consistency):
<%= @full_content %>
<% end %>
<% else %>
<%= @content %>
<% end %>

<% if @target_length.present? %>
Target length: <%= @target_length %>
<% end %>

<% if @areas_to_expand.present? %>
Areas to focus on: <%= @areas_to_expand %>
<% end %>

Expand the content while maintaining the original tone and style. Add relevant details, examples, and explanations to make the text more comprehensive.
<% if @has_selection %>Only output the expanded selected text, not the entire document.<% end %>
